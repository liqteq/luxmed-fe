import React from 'react'
import { AestheticFormProps } from '../types'

const AestheticForm: React.FC<AestheticFormProps> = (props) => {
    const { control } = props

    return (
        <div>AestheticForm</div>
    )
}

export default AestheticForm